import React from "react";

const About = () => {
  return (
    <div>
      <h1>About This App</h1>
      <p>
        This is the get started app based on React framework. It covers all the
        basic concepts such as functional components and the class components.
        It also illustrates the basic routing with single component as well as
        for more than one components...
      </p>
    </div>
  );
};

export default About;
